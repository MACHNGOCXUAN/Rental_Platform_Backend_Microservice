name: Deploy Estate Service

on:
  push:
    branches:
      - main
    paths:
      - 'apps/estate-service/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Image
        run: |
          docker build -t xuanit/estate-service:latest ./apps/estate-service
          docker push xuanit/estate-service:latest

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST_ESTATE }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~

            # check env tồn tại
            if [ ! -f .env ]; then
              echo ".env file not found!"
              exit 1
            fi

            docker pull xuanit/estate-service:latest

            docker stop estate-service || true
            docker rm estate-service || true

            docker run -d \
              --name estate-service \
              -p 9001:9001 \
              --env-file .env \
              xuanit/estate-service:latest