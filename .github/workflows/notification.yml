name: Deploy Notification Service

on:
  push:
    branches:
      - main
    paths:
      - 'apps/notification-service/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Image
        run: |
          docker build -t xuanit/notification-service:latest ./apps/notification-service
          docker push xuanit/notification-service:latest

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST_NOTIFICATION }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~

            # check env tồn tại
            if [ ! -f .env ]; then
              echo ".env file not found!"
              exit 1
            fi

            docker pull xuanit/notification-service:latest

            docker stop notification-service || true
            docker rm notification-service || true

            docker run -d \
              --name notification-service \
              -p 9004:9004 \
              --env-file .env \
              xuanit/notification-service:latest